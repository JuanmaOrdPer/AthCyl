/**
 * Polyfill para BlobManager de React Native
 */

const BlobManager = {
  isAvailable: false,
  addWebSocketHandler: () => {},
  removeWebSocketHandler: () => {},
  sendOverSocket: () => {},
  createFromOptions: () => ({}),
};

export default BlobManager;
