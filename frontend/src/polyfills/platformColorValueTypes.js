/**
 * Polyfill para PlatformColorValueTypes de React Native
 */

export const processColorObject = (color) => {
  return color;
};

export const normalizeColorObject = (color) => {
  return color;
};
